# 部署指南

## 🚀 快速部署到 Netlify

本指南将帮助你将王一然个人简历网站部署到 Netlify 平台。

## 前置要求

- ✅ Node.js 18+ 已安装
- ✅ Git 已安装
- ✅ Netlify 账号（可免费注册）
- ✅ Netlify CLI（可选，用于命令行部署）

## 方式一：通过 Netlify 网站部署（推荐新手）

### 1. 准备代码仓库

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 王一然个人简历网站"

# 连接到远程仓库（GitHub/GitLab/Bitbucket）
git remote add origin <your-repo-url>
git push -u origin main
```

### 2. 在 Netlify 创建站点

1. 访问 [Netlify](https://app.netlify.com/)
2. 点击 "Add new site" → "Import an existing project"
3. 选择你的 Git 提供商（GitHub/GitLab/Bitbucket）
4. 授权并选择你的仓库
5. 配置构建设置：
   - **Build command**: `npm run build` 或 `pnpm build`
   - **Publish directory**: `.next`
   - **Functions directory**: (留空)

6. 点击 "Deploy site"

### 3. 等待部署完成

- 首次部署通常需要 2-3 分钟
- 部署完成后会显示网站地址（如 `https://your-site.netlify.app`）

### 4. 自定义域名（可选）

1. 在 Netlify 站点设置中找到 "Domain management"
2. 点击 "Add custom domain"
3. 输入你的域名（如 `wangyiran.com`）
4. 按照提示配置 DNS 记录

## 方式二：通过 Netlify CLI 部署（推荐开发者）

### 1. 安装 Netlify CLI

```bash
# 全局安装
npm install -g netlify-cli

# 或使用 pnpm
pnpm add -g netlify-cli
```

### 2. 登录 Netlify

```bash
netlify login
```

这会打开浏览器窗口，完成授权后返回终端。

### 3. 初始化站点

```bash
# 在项目根目录执行
netlify init
```

选择：
- "Create & configure a new site"
- 选择你的团队
- 输入站点名称（可选）

### 4. 构建项目

```bash
# 使用 npm
npm run build

# 或使用 pnpm
pnpm build
```

### 5. 部署到 Netlify

```bash
# 部署到生产环境（无构建）
netlify deploy --prod --no-build
```

或者使用两步部署（推荐，避免超时）：

```bash
# 第一步：构建
npm run build

# 第二步：部署
netlify deploy --prod --no-build
```

### 6. 验证部署

部署成功后，CLI 会显示网站地址。访问该地址检查网站是否正常。

## 方式三：手动拖拽部署

### 1. 构建项目

```bash
npm run build
```

### 2. 打包构建产物

将 `.next` 文件夹压缩成 zip 文件。

### 3. 上传到 Netlify

1. 访问 [Netlify Drop](https://app.netlify.com/drop)
2. 拖拽 zip 文件到页面
3. 等待上传和部署完成

## 环境变量配置

本项目不需要环境变量，但如果将来需要，可以在 Netlify 设置：

1. 进入站点设置
2. 找到 "Environment variables"
3. 添加所需的环境变量

## 构建配置说明

项目的 `netlify.toml` 文件包含以下配置：

```toml
[build]
  publish = ".next"
  command = "npm run build"
```

- **publish**: 指定发布目录为 `.next`（Next.js 构建输出）
- **command**: 指定构建命令

## 常见问题

### Q1: 部署后页面空白或404

**原因**: 可能是路由配置问题或构建失败

**解决方案**:
1. 检查 Netlify 构建日志
2. 确认 `next.config.js` 配置正确
3. 确认 `netlify.toml` 中的 publish 目录是 `.next`

### Q2: 图片无法显示

**原因**: Next.js 图片优化在静态导出时需要特殊配置

**解决方案**:
- 已在 `next.config.js` 中配置 `images: { unoptimized: true }`
- 确认图片路径正确（使用 `/image/xxx.png`）

### Q3: 构建超时

**原因**: 构建时间超过 Netlify 限制

**解决方案**:
- 使用两步部署：先本地构建，再部署
```bash
npm run build
netlify deploy --prod --no-build
```

### Q4: CSS 样式丢失

**原因**: Tailwind CSS 配置问题

**解决方案**:
- 检查 `postcss.config.js` 是否存在
- 确认 `globals.css` 中有 `@tailwind` 指令
- 清除缓存重新构建

### Q5: 动态路由不工作

**原因**: Next.js App Router 需要特殊配置

**解决方案**:
- 本项目使用静态页面，无动态路由
- 如需动态路由，考虑使用 Netlify Functions

## 性能优化建议

### 1. 图片优化

- ✅ 已使用 Next/Image 组件
- ✅ 已配置懒加载
- 建议：压缩证书图片到合理大小（< 500KB）

### 2. 代码分割

- ✅ Next.js 自动代码分割
- ✅ 动态导入已配置

### 3. 缓存策略

Netlify 自动配置缓存，无需额外设置。

### 4. CDN 加速

Netlify 提供全球 CDN，自动启用。

## 监控和维护

### 1. 部署状态

在 Netlify Dashboard 可以查看：
- 部署历史
- 构建日志
- 访问统计

### 2. 自动部署

配置 Git 集成后：
- 每次 push 到 main 分支自动触发部署
- 可以设置部署预览（Pull Request）

### 3. 回滚

如果新部署出现问题：
1. 进入 Netlify Dashboard
2. 找到 "Deploys" 标签
3. 选择之前的成功部署
4. 点击 "Publish deploy"

## 更新内容

当需要更新网站内容时：

### 更新奖项

1. 编辑 `app/achievements/page.jsx`
2. 添加新的奖项数据
3. 添加证书图片到 `image/` 文件夹
4. 提交并推送（自动部署）或手动部署

### 更新作品

1. 编辑 `app/works/page.jsx`
2. 添加新的项目信息
3. 提交并推送

### 更新诗歌

1. 编辑 `poem.md`
2. 添加新的诗歌内容
3. 提交并推送

## 成本

- **免费套餐**: 
  - ✅ 100GB 带宽/月
  - ✅ 300 分钟构建时间/月
  - ✅ 自定义域名
  - ✅ HTTPS 证书
  - ✅ 足够个人网站使用

- **付费套餐**: 如果流量增大，可升级到 Pro 套餐（$19/月）

## 自定义域名配置

如果你有自己的域名（如 `wangyiran.com`）：

### 1. 在 Netlify 添加域名

```
Domain management → Add custom domain → 输入域名
```

### 2. 配置 DNS 记录

在你的域名注册商处添加以下记录：

**方式 A: 使用 Netlify DNS（推荐）**
- 将域名的 Nameservers 指向 Netlify 提供的地址

**方式 B: 使用 CNAME 记录**
```
类型: CNAME
名称: www
值: your-site.netlify.app
```

### 3. 启用 HTTPS

Netlify 会自动为自定义域名配置 Let's Encrypt SSL 证书，通常在几分钟内完成。

## 命令速查表

```bash
# 开发
npm run dev              # 启动开发服务器
netlify dev             # 使用 Netlify 环境启动

# 构建
npm run build           # 构建生产版本

# 部署
netlify deploy          # 部署到预览环境
netlify deploy --prod   # 部署到生产环境

# 两步部署（推荐）
npm run build
netlify deploy --prod --no-build

# 查看站点
netlify open            # 打开 Netlify Dashboard
netlify open:site       # 打开已部署的网站

# 查看日志
netlify logs            # 查看函数日志

# 链接站点
netlify link            # 链接到现有站点
netlify unlink          # 取消链接
```

## 故障排除清单

部署前检查：

- [ ] 所有依赖已安装（`npm install`）
- [ ] 本地构建成功（`npm run build`）
- [ ] 没有 TypeScript/ESLint 错误
- [ ] 所有图片资源存在
- [ ] `netlify.toml` 配置正确
- [ ] `next.config.js` 配置正确

部署后检查：

- [ ] 首页可以访问
- [ ] 所有导航链接工作
- [ ] 图片正常显示
- [ ] 动画效果正常
- [ ] 移动端显示正常
- [ ] 控制台无错误

## 技术支持

如遇到问题：

1. **查看构建日志**: Netlify Dashboard → Deploys → 点击部署 → 查看 "Deploy log"
2. **Netlify 文档**: https://docs.netlify.com/
3. **Next.js 文档**: https://nextjs.org/docs
4. **社区支持**: 
   - Netlify Community: https://answers.netlify.com/
   - Next.js Discussions: https://github.com/vercel/next.js/discussions

## 下一步

部署完成后：

1. ✅ 测试所有页面功能
2. ✅ 分享网站链接给老师和同学
3. ✅ 定期更新内容
4. ✅ 监控访问统计
5. ✅ 收集反馈并改进

---

**祝部署顺利！🎉**

如有问题，请参考本文档或联系技术支持。

